[pytest]
# Collect from tests/ only. The project root holds application modules, and a
# future tool named *_test.py would otherwise be imported and run as a test --
# which is exactly what happened in the face-recognition project.
testpaths = tests
